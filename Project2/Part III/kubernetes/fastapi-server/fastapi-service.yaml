apiVersion: v1
kind: Service
metadata:
  name: fastapi-server-deployment
spec:
  selector:
    app: fastapi-server-deployment
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
