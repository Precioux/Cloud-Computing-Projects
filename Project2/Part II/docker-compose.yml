version: "3.9"

services:
  redis:
    build: ./redis
    container_name: redis
    ports:
      - "6379:6379"

  server:
    build: ./server
    container_name: server
    ports:
      - "8000:8000"
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis
